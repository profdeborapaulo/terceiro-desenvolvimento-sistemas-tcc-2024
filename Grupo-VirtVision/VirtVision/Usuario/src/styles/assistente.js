import {StyleSheet} from 'react-native';

const styleAssistente = StyleSheet.create({
    background: {
      flex:1,
      alignItems: 'center',
      justifyContent: 'center',
      
    },

text: {
    fontSize: 25,
    fontWeight: 'bold'
}

});

export default styleAssistente;